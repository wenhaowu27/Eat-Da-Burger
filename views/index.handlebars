

<main style="margin-left: 350px">
  <h1>Eat-Da-Burger!</h1>
  <div class="container">
    <div class="row">
      <div class="col s6">
        {{#each burger}}
          {{#unless this.devoured}}
            <div>
              <form action="/burger/{{this.id}}?_method=PUT" method="POST">
                <button type="submit" class='burgerAvailable'> {{this.burger_name}}</button>
              </form>
            </div>
          {{/unless}}
        {{/each}}
      </div>

      <div class="col s6">
        {{#each burger}}
          {{#if this.devoured}}
            <div class='burgerEaten'>
              <div>
                {{this.burger_name}}
              </div>
            </div>
          {{/if}}
        {{/each}}
      </div>
    </div>
  </div>
</main>

<h2 style="margin-left: 300px;">Add A Burger Option</h2>

<div class="row">
  <div class="col s12">
    <form action="/burger" method="POST">
      <input type="text" id="name" name="burger_name" placeholder="Uber Delicious Burger" style="margin-left: 300px;">
      <button type="submit" class="addBurgerButton">Add The Burger</button>
    </form>
  </div>
</div>